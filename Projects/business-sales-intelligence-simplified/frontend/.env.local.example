# URL of the FastAPI backend (see ../backend).
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000

# After deploying the backend to Render, set this in Vercel's
# Project Settings -> Environment Variables to your Render URL, e.g.:
# NEXT_PUBLIC_API_URL=https://business-sales-intelligence-api.onrender.com
